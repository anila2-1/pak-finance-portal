import type { Config } from 'tailwindcss'

const config: Config = {
  // In Tailwind CSS v4, theme customization is done via CSS @theme directives.
  // This file is primarily used for registering plugins and custom utilities.
}

export default config
